[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    slow: Tests that are slow (real integration, full server setup)
    flaky: Tests that are flaky under parallel execution
