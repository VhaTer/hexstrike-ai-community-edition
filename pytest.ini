[pytest]
# FastMCP real-world and legacy Flask test — ignored (require server or obsolete)
addopts = --ignore=tests/test_endpoints_exist.py --ignore=tests/test_fastmcp_real_world.py
testpaths = tests
pythonpath = .
